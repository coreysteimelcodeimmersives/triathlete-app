import { Box } from '@mui/system';
import React from 'react';
import Layout from '../Components/Layout/Layout';

const TuneFilterWoLib = () => {
  return (
    <Layout>
      <Box>This is where we are gonna filter and tune wo lib</Box>
    </Layout>
  );
};

export default TuneFilterWoLib;
